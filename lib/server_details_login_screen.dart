import 'package:flutter/material.dart';

class ServerDetailsLoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Server Connection Details'),
        centerTitle: true,
      ),
      body: Center(
        child: Text("Something will go here."),
      ),
    );
  }
}